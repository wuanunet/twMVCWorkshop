@model string
@{
    var id = Guid.NewGuid();
    var skin = ViewData["skin"] ?? "kama";
}
@Html.TextArea("", Model, new { @id = id })
@*@Html.TextArea("", Model, new { id })*@
<script type="text/javascript">
    (function() {
        var editor = CKEDITOR.editor.replace('@id', { skin: '@skin' });
    })();
</script>